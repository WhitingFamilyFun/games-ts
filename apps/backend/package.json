{
  "name": "@games/backend",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "test": "vitest run",
    "test:integration": "FIREBASE_DATABASE_EMULATOR_HOST=127.0.0.1:9000 vitest run src/integration.test.ts"
  },
  "dependencies": {
    "@games/effect-schemas": "workspace:*",
    "@games/game-engine": "workspace:*",
    "effect": "^3.12.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.6.0"
  }
}
